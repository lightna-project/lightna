{
    "config": {
        "process-timeout": 0
    },
    "require": {
        "laminas/laminas-db": "^2.20"
    },
    "scripts": {
        "lightna.build": [
            "./cli build.compile",
            "npm run build",
            "./cli build.asset.sign",
            "./cli build.apply",
            "./cli update",
            "./cli index.update.all"
        ]
    }
}
