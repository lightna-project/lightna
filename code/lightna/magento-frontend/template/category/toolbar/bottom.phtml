<?php
/**
 * Import:
 * @var Lightna\Engine\Data\Url $url
 * @var Lightna\Magento\Backend\Data\Product\Search $search
 */

$lastPage = ceil($search->total / $search->pageSize);
?>
<div class="toolbar">
    <?php for ($i = 1; $i <= $lastPage; $i++) { ?>
        <a href="<?= escape($url->current(['p' => $i])) ?>"><?= escape($i) ?></a>
    <?php } ?>
</div>
