<?php
/**
 * Import:
 * @var Lightna\Magento\Backend\Data\Product\Search $search
 */

$posFrom = ($search->currentPage - 1) * $search->pageSize + 1;
$posTo = min($search->currentPage * $search->pageSize, $search->total);
?>
<?= phrase('Items %1-%2 of %3', [$posFrom, $posTo, $search->total]) ?>
